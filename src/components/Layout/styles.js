const getStyles = ({ theme }) => ({
  container: {
    width: "100%",
    height: "100vh",
    padding: "2rem",
    background:
      "linear-gradient(90deg, rgba(238,174,202,1) 0%, rgba(48,137,241,1) 100%)",
  },
  content: {
    maxWidth: "60rem",
    margin: "auto",
  },
});

export default getStyles;
